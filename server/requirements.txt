# Runtime deps for the hosted webapp backend (NOT the zero-dep CLI core).
# matplotlib is deliberately excluded — Recharts renders charts in the SPA.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
slowapi>=0.1.9
